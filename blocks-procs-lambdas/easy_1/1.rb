class Tree
  include Enumerable

  def each
    # implementation not shown. to access most functionality of Enumerable, define an each method that yields each member of collection, one at a time
  end
end